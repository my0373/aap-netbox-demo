# netbox_inventory.yml  (no Jinja here)
plugin: netbox.netbox.nb_inventory

validate_certs: true
config_context: false

group_by:
  - device_roles
  - sites
  - platforms

query_filters:
  - status: active

device_query_filters:
  - has_primary_ip: "true"

